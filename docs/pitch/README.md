# Editable pitch

60 seconds, 1080p, English. ElevenLabs Tommy (Multilingual v2), generated in the project owner’s account. Captions use word timestamps with spelling corrected against the script.

```bash
npm run check
npm run dev
npm run render -- --output ../../public/secondlook-pitch-v2.mp4 --quality high --fps 30
```

Edited captures of the real deployed application, not continuous screen recording. The same demo assessment demonstrates free research, an SDK failed test purchase, a valid test purchase, and the generated evidence PDF. No real charge. The closing scene shows the PDF preview only; no email inbox or confirmation is shown. Original audio and voice provenance are included.

The pre-sprint video and intermediate drafts remain in the sibling `secondlook-demo-video` workspace.
